import * as ActionTypes from './ActionTypes';

export const Cart = ( state = {
    cart: [],
    errMess: ''
}, action) => {
    switch (action.type) {
        default:
            return state;
    }
};
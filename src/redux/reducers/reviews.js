import * as ActionTypes from '../ActionTypes';

export const Reviews = (state = {
    isLoading: true,
    errMess: null,
    reviews: []
}, action) => {
    switch (action.type) {
        default:
            return state;
    }
};
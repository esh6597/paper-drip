import React, { Component } from 'react';
import { baseUrl } from '../shared/baseUrl';

function Blog(props) {
    return (
        <div>
            <h1>Blog</h1>
        </div>
    );
}

export default Blog;